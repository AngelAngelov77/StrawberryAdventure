using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace StrawberryAdventure
{
    public class MapGenerator
    {

        // Can also set the color of the Console to different color for each texture ( once changed, the color stays the same for every other texture, so you have to change it everytime )
        private const string MapTexture = "-";

        public void DrawMap()
        {
            
        }
    }
}
